version: '3.8'

services:
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    container_name: week9-api

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    container_name: week9-redis
